<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.project">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission. ACCESS_COARSE_LOCATION" />

    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="Circle 17"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBtXkHfgQxnxw-6aYQ8QcojZ4LIUe5NXwo" />

        <activity
            android:name=".Activity.Activity_Maps"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".Activity.Activity_Register"
            android:label="Register" />
        <activity
            android:name=".Activity.Activity_Home"
            android:label="Home" />
        <activity
            android:name=".Activity.Activity_List_Conversations"
            android:label="Conversations" />
        <activity
            android:name=".Activity.Activity_Chat"
            android:label="Chat" />
        <activity
            android:name=".Activity.Activity_Profile"
            android:label="Profile" />
        <activity
            android:name=".Activity.Activity_Create_Circle"
            android:label="Create Circle" />
        <activity
            android:name=".Activity.Activity_MyCircle_Home"
            android:label="My Circle Home" />
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>